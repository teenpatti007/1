{
  "rules": {
    "passcodes": {
      ".read": true,
      "$code": {
        ".write": "auth != null || (data.exists() && data.child('active').val() == true && newData.child('active').val() == true && (!data.hasChild('device') || data.child('device').val() == newData.child('device').val()))"
      }
    }
  }
}
